name: security-audit

on:
  pull_request:
  schedule:
    - cron: "0 5 * * 1"

jobs:
  static-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Static security audit (XSS / secrets / CORS / CSP / links)
        run: node tools/security-audit.cjs

  npm-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install frontend
        run: npm ci

      - name: Audit frontend prod dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Install backend
        run: npm --prefix backend ci

      - name: Audit backend prod dependencies
        run: npm --prefix backend audit --omit=dev --audit-level=high
